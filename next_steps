#Other
dev tree memory node sets aside a few MB

#C Prog
starts PRU firmware
program mmaps the memory node address
zero the memory space
passes pointer to PRU via RPmesg
tells pru to start transfer via RPmesg
waits for finish signal from PRU via RPmesg
reads from address

#PRU Prog
waits for pointer and signal from C prog via RPmesg
starts transfer to pointer
once finished, singal c prog via RPMesg

#Other
enable UART debugging on PRU
enable parallel capture on PRU

